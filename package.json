{
  "name": "pivori-studio-v2",
  "version": "2.0.0",
  "description": "PIVORI Studio v2.0 - Advanced Financial Dashboard",
  "private": true,
  "type": "module",
  "scripts": {
    "install:all": "npm install && cd frontend && npm install --legacy-peer-deps && cd ../backend && npm install --legacy-peer-deps && cd ..",
    "build": "cd frontend && npm run build",
    "dev": "cd frontend && npm run dev",
    "test": "cd frontend && npm run test",
    "lint": "cd frontend && npm run lint",
    "start": "cd backend && npm start"
  },
  "dependencies": {},
  "devDependencies": {},
  "engines": {
    "node": ">=22.0.0",
    "npm": ">=10.0.0"
  }
}

